# Claude API (for question + theme generation)
ANTHROPIC_API_KEY=

# Neon PostgreSQL (for results persistence)
DATABASE_URL=

# Results TTL (days before results links expire)
RESULTS_TTL_DAYS=7

# PartyKit (realtime multiplayer)
# Local dev: localhost:1999. Production: your-app.partykit.dev
NEXT_PUBLIC_PARTYKIT_HOST=localhost:1999
